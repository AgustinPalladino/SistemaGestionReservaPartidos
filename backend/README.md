# Backend

Solución: `SistemaGestionReservaPartidos.sln`

```powershell
# Compilar
dotnet build

# Ejecutar API
cd src\SistemaGestion.API
dotnet run
```

Cadena de conexión: `src\SistemaGestion.API\appsettings.json` o variable de entorno `DATABASE_URL`.
